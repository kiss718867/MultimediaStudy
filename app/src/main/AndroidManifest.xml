<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gg.multimediastudy">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MultimediaStudy">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".h264player.H264PlayerActivity" />
        <activity android:name=".h264encoder.H264EncoderActivity" />
        <activity android:name=".h264parser.H264ParseActivity" />
        <activity android:name=".camera1capture.Camera1CaptureActivity" />
        <activity android:name=".screenprojection.ScreenProjectorActivity" />
        <activity android:name=".screenprojection.ScreenProjectionPlayerActivity" />
        <activity android:name=".h265videocall.VideoCallServerActivity" />
        <activity android:name=".h265videocall.VideoCallClientActivity" />
    </application>

</manifest>